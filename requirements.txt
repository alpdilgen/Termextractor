# Streamlit Cloud Requirements
# Minimal dependencies for web app deployment

# Core - Anthropic API
anthropic>=0.34.0

# Web Framework
streamlit>=1.31.0

# Data Processing (minimal)
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# Document Processing (essential only)
python-docx>=1.1.0 # Still needed if other parts use it, or can be removed if ONLY docx2txt is used for docx
PyPDF2>=3.0.0
lxml>=5.0.0
beautifulsoup4>=4.12.0
docx2txt # Added for robust .docx parsing

# Configuration
python-dotenv>=1.0.0
PyYAML>=6.0.1
pydantic>=2.5.0

# Logging
loguru>=0.7.0

# Utilities
python-dateutil>=2.8.2
keyring # Added for secure storage

# Security
cryptography>=42.0.0 # Make sure this is present

# Add any other required libraries like striprtf if you implement RTF parsing
